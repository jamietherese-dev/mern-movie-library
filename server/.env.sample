# This is a sample .env file for use in local development.
# Duplicate this file as .env in the server folder of the project
# and update the environment variables to match your
# desired config

DB_URI="mongodb://127.0.0.1:27017/movies"
